{
  "buildCommand": "cd angeles-client && npm install && npm run build",
  "outputDirectory": "angeles-client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}